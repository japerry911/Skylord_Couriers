import React from 'react';
import { useStyles } from './HomeStyles';

const Home = () => {
    const classes = useStyles();

    return (
        <div>
            <h1>Homepage</h1>
        </div>
    );
};

export default Home;